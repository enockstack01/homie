<Window x:Class="xGIS.AddIn.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="xGIS Settings"
        Width="440" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="xGIS API key" FontWeight="Bold" Margin="0,0,0,4" />

        <Grid Grid.Row="1" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <PasswordBox x:Name="ApiKeyPasswordBox" Grid.Column="0" VerticalContentAlignment="Center" />
            <TextBox x:Name="ApiKeyTextBox" Grid.Column="0" Visibility="Collapsed" VerticalContentAlignment="Center" />

            <CheckBox x:Name="ShowKeyCheckBox" Grid.Column="1" Content="Show" Margin="8,0,0,0"
                      VerticalAlignment="Center" Checked="ShowKeyCheckBox_Changed" Unchecked="ShowKeyCheckBox_Changed" />
        </Grid>

        <TextBlock Grid.Row="2" x:Name="ApiKeyStatusText" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,4" />
        <TextBlock Grid.Row="3" FontSize="11" Foreground="Gray" TextWrapping="Wrap" Margin="0,0,0,12"
                   Text="Ask your xGIS administrator for an API key (starts with 'ak_'). This is not an Anthropic key - xGIS never sees that directly, and this key isn't something you can self-generate from a personal account." />

        <TextBlock Grid.Row="4" FontSize="11" Foreground="Gray" TextWrapping="Wrap" Margin="0,0,0,12"
                   Text="Model choice now lives in your Homie account dashboard, not here - sign in there to pick which model your key uses." />

        <TextBlock Grid.Row="5" Text="Backend URL" FontWeight="Bold" Margin="0,0,0,4" />
        <TextBox Grid.Row="6" x:Name="BackendUrlTextBox" Margin="0,0,0,12" />

        <CheckBox Grid.Row="7" x:Name="ConfirmCheckBox"
                  Content="Confirm before destructive or out-of-sandbox operations"
                  Margin="0,0,0,16" />

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" Width="90" Margin="0,0,8,0" Click="Close_Click" IsCancel="True" />
            <Button Content="Save" Width="90" Click="Save_Click" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
